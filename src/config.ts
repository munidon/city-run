export const GAME_WIDTH = 1280;
export const GAME_HEIGHT = 720;
export const PARENT_ID = "game";
export const BACKGROUND_COLOR = "#1a1a2e";

export const GROUND_Y = Math.round(GAME_HEIGHT * 0.75);
export const GROUND_HEIGHT = GAME_HEIGHT - GROUND_Y;
export const PLAYER_X = Math.round(GAME_WIDTH * 0.35);
export const BASE_SPEED = 280;

export const SPAWN_X = GAME_WIDTH + 80;
export const DESPAWN_X = -200;
